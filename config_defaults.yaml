# Todoist API Configuration
# Rename this file to config.yaml and set your Todoist API token
todoist:
  
  api_token: "REPLACE_WITH_YOUR_TODOIST_API_TOKEN"
  base_url: "https://api.todoist.com/rest/v2"

# Service Configuration
server:
  port: "8080"
